{{ form_start(form, { attr: { class: 'row g-3 justify-content-center' } }) }}
    <div class="row g-3 justify-content-center">    
        {{ form_row(form.url) }}
        {{ form_row(form.timeToLive) }}
    </div>
    <div class="row">
        {{ form_row(form.submit) }}
    </div>
{{ form_end(form) }}